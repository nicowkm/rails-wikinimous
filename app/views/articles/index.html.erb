<h3>My Article Home-page <%= link_to "Add a new Article",new_article_path%></h3>


<div>
<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article)%> </li>
    <li><%= article.content %></li>
  <% end %>
</ul>
</div>
